2026-08-20 [preregistered-reading-mismatched-outcome] Pre-registered a reading for "both inconclusive" that assumed inconclusive meant a SMALL effect; the run came back inconclusive with a large effect failing on sign consistency instead, so the pre-committed interpretation did not fit the outcome it was written for and had to be marked wrong rather than applied. — ref: #559 / injection re-run
