2026-08-20 [verification-scoped-to-wrong-target] Three checks in one session each returned a clean result because it queried the wrong scope rather than because the thing was clean — a grep whose unquoted `--include=*.py` zsh consumed, so the command errored and an `||` fallback printed "ZERO HITS ✓"; a JSON probe reading the wrong nesting level that reported 0 of 51 rows carrying a field the same payload contained 91 times; and a mutation run executed against a test file that did not contain the test it was meant to kill, which briefly read as "the change is unverified" on a correct PR. — ref: #600 / PR #637
