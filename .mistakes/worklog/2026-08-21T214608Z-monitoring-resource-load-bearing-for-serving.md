2026-08-21 [monitoring-resource-load-bearing-for-serving] a cost cut set `--min-instances 0` on the web tier and left cold-start latency depending on a /health uptime check staying alive — the dependency was recorded only in a deploy-yml comment, no alarm covered the ~10.8s regression it predicted, and it surfaced as a user reporting the site "not loading" — ref: branch claude/gridpulse-loading-issue-stagoh
