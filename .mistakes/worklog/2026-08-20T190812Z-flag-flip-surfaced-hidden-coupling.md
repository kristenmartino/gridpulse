2026-08-20 [flag-flip-surfaced-hidden-coupling] Turning a dark feature flag on broke an unrelated test that asserted redis_set.call_count == 1 — a fact about which enrichment flags happen to be enabled, not about the model metrics under test — and revealed that 44-of-51 never-gapping BAs would re-persist an empty payload every hour; neither was visible while the flag was off. — ref: #559 / seed shadow flip
