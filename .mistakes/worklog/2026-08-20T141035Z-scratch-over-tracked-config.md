- 2026-08-20 [scratch-over-tracked-config] Wrote a session-scratch dev-server config over `.claude/launch.json` in a worktree on the assumption it was untracked scratch — it is a tracked repo file (and the preview tool had ignored it anyway, launching the main repo's server config instead); caught as a spurious deletion in `git status` before commit. — ref: branch worktree-benchmark-page-positioning
