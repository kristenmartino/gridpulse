2026-08-20 [sufficiency-not-checked] Claimed in a docstring that 'warn < gate' proved a warning always fires before its exclusion, and wrote a test asserting exactly that comparison — but the alert reads payloads back from Redis, so the real requirement is that a payload cannot go staler than the band is wide. The two constants live in different files and nothing connected them. Found by an adversarial review, not by the tests. — ref: #587
