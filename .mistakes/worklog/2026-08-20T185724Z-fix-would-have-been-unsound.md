2026-08-20 [fix-scope-underestimated] The obvious #559-candidate-1 fix (relax the origin cap alone) would have advanced the origin past hours the seed still indexes positionally, and the hour-indexed variant would have imputed hours we hold — 691 MW off on `demand_lag_1h`, caught only by measuring the lags at the new origin before writing the code — ref: branch fix/forecast-origin-stall-559, #559.
