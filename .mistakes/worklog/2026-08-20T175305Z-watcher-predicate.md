- 2026-08-20 [watcher-predicate] Armed a deploy watcher that grepped the Cloud Run image tag for a literal SHA, while knowing the deployed tag moves past your commit when concurrent PRs merge — a sibling session's #612 deployed first and my #613's SHA would never have appeared, so the watcher would have waited out its timeout and read as "never deployed". The ancestry-vs-equality rule was applied when reading the result but not when writing the check. — ref: PR #613 / #614
