2026-08-20 [inherited-policy-not-decided] Shipped a fix whose absent-hour behaviour was never chosen: the temporal snapshot returns NaN for a missing lag hour and the shared row.fillna(0) turns it into demand_lag = 0 MW, the #129 poison the seed filter exists to exclude — 13% of forecast steps, 22.6% on IID, and it is the likeliest reason the pre-registered study came back against its own hypothesis. Flagged as an unmeasured limit in the prior study rather than resolved. — ref: #559 / PR #584 / injection study
