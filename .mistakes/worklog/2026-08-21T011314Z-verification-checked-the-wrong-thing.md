2026-08-21 — near-miss on #600: a live "fleet must recompute from rows" checker handed to me reported 4 mismatches on `/api/v1/benchmark`, but 2 of them were `fleet_rollup`'s deliberate ERCOT isolation rather than the tick-mixing bug (the min-side deltas, including the headline "0.498, 26% relative" figure), so building the characterization test to that predicate would have shipped an acceptance check that can never pass; caught by recomputing both definitions against a production capture before writing the claim down.
