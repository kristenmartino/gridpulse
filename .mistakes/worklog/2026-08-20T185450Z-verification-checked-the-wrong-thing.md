- 2026-08-20 [verification-checked-the-wrong-thing] A vintage probe printed a column labelled `placeholder (d==df at first sight)` that actually computed `df.notna().sum()` — it reported "72 placeholder hours" for JEA when the real count of `d == df` was 0, and would have handed the #539 placeholder disclosure a fabricated instance had I read the label instead of the raw rows. — ref: PR for docs/DRIFT_COVERAGE_CHANNELS.md
