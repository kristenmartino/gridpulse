- 2026-08-20 [verification-instrument] A deploy watcher reported "DEPLOYED" from `spec.template.spec.containers[0].image`, which flips when a deploy is *requested*; the new revision was still failing its health check and 100% of traffic sat on the previous revision, so the live page served the old copy while the watcher said success. Caught by curl'ing the page, not by the watcher. Same watcher, second wrong predicate in one session (the first was equality-vs-ancestry). — ref: PR #617
