"""
Dashboard UI components.

Active Tabs:
    tab_forecast — Historical Demand
    tab_demand_outlook — Demand Forecast (forward-looking)
    tab_backtest — Backtest (model evaluation)

Core:
    layout — Main dashboard layout (header, persona switcher, 3-tab container)
    callbacks — All Dash callbacks (21 groups)
    cards — KPI cards, welcome cards, alert cards
    error_handling — Confidence badges, loading states
    accessibility — Colorblind palette, ARIA helpers

Dormant Tabs (code preserved):
    tab_weather, tab_models, tab_generation, tab_alerts, tab_simulator
"""
